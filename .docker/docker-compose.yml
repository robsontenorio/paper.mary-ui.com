services:
    paper:
        build:
            context: ..
            dockerfile: .docker/Dockerfile
            target: local
        volumes:
            - ../:/var/www/app:cached
        ports:
            - 8016:8080
            - 5176:5176
